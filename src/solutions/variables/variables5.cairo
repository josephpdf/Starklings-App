fn main() {
    let number = 1_u8; // don't change this line
    println!("number is {}", number);
    let number = 3; // don't rename this variable - Se asigna la variable
    println!("number is {}", number);
}
